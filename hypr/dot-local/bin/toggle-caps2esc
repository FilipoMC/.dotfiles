#!/usr/bin/env bash

if [ "$(systemctl is-active interception-tools)" = "active" ]; then
  sudo systemctl stop interception-tools
else
  sudo systemctl start interception-tools
fi

pkill -RTMIN+9 waybar
